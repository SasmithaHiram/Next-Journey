const AddToCart = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("Clciked");
        }}
      >
        Add To Cart
      </button>
    </div>
  );
};

export default AddToCart;
